<section>
    <h3>Comments</h3>
    <article>
        <p>{{comment}}</p>
        <p>{{username}}</p>
        <p>{{date}}</p>
        <input type="hidden" id="post_id" value="{{id}}">
    </article>
</section>